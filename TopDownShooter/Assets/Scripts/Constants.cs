public class Constants{
    //dont touch danger!!!
    public static System.Boolean gameOver = false;

    //player variables; if you make it too large bad things happen so don't
    public static float movespeed = (float)0.1;
}